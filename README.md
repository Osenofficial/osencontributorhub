# OSEN Contributor Hub

Monorepo for the OSEN contributor platform (frontend + backend).

## Documentation

- **[Platform user guide (roles, features, how to use)](docs/PLATFORM_USER_GUIDE.md)** — for contributors, leads, and admins.

## Development

See `Backend/README.md` and `Frontend/package.json` for run scripts. Configure `Frontend/.env` with `NEXT_PUBLIC_API_BASE_URL` pointing at your API.